定义网格：输入内圆半径230mm，外圆半径920mm，径向的元素数量8，圆周元素数量16
最后储存在nodes和elements两个数列之中 也就是后面的x和elems
形函数：xi和eta  等参单元的自然坐标 [-1,1]是用于八节点二次等参单元（quadratic isoparametric element）的,返回一个包含八个形函数值得张量N，(修改了四个角节点的定义按照文档修改)
形函数的导数：(修改)
高斯积分点：按照文档更改了权重为5/9，8/9，5/9
雅可比矩阵没有更改
然后直接分析函数：材料参数E,nu  每个节点的自由度是ndf=2,每个单元的节点数8,以及一些尺寸规定
    生成网格：x是储存节点坐标，elems是单元连接矩阵
    绘制初始的网格：遍历所有的单元elems，将张量转换成numpy数组
    提取单元中第一个、第二个、第三个和第四个节点的 x 坐标和y坐标用蓝色的实线绘制
    所有的单元的角节点和边界点用红色虚线连接到一起形成闭合路径
    最后所有的节点位置都用红点显示
    定义边界条件：固定内节点 drlt的两个维度x，y方向为0  (修改drlt和neum矩阵的初始设置)
                 定义负载  Na和Fa是垂直载荷和铁轨和车轮的摩擦力 Nb和Fb是闸瓦给的摩擦力  (定义两边节点where条件进行调整)
                 初始化应力和应变列表
                 定义外部载荷 把负载定义到neum矩阵(目前能配置成功 三个节点)
    求解过程：高斯点和权重(都是9行的 多一个9节点，暂时不知道如何去掉)
    初始化刚度矩阵K(1312*1312)和外力F(1312) 
    开始应用边界条件和负载  把drlt和neum矩阵中数值引入到K和F中(暂时没研究明白，感觉问题可能在这里以及F的初始化大小)
    先是节点node循环然后连接矩阵elems循环, 连接矩阵循环下的子循环是高斯矩阵形函数雅可比矩阵对8个节点进行组装 得到每个连接矩阵的局部刚度矩阵ke
    然后一个在nen=8循环组装全局的刚度矩阵(我觉得循环的range应该是在elems=128的个数，不是nen)
    矩阵方程求解u=K\F(初步运行了一下显示矩阵是奇异矩阵无法求解) 然后对位移进行后处理，在连接矩阵之间进行循环求解应力应变(内部的公式还没完全研究明白，plot出图方面应该没什么问题)
 